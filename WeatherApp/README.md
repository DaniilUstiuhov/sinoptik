# Weather App - Погодное Android приложение

## 📱 Описание

Android-приложение для отображения текущей погоды и 5-дневного прогноза с использованием OpenWeather API.

### Возможности:
- ✅ Отображение текущей погоды (температура, влажность, ветер, давление)
- ✅ 5-дневный прогноз погоды
- ✅ Выбор города
- ✅ Переключение единиц измерения (°C / °F)
- ✅ Светлая / тёмная тема
- ✅ Настройка размера шрифта (маленький / средний / большой)
- ✅ Pull-to-refresh для обновления данных
- ✅ Обработка ошибок (нет интернета, город не найден)

---

## 🚀 Инструкция по запуску

### Шаг 1: Получение API ключа

1. Перейдите на сайт [OpenWeatherMap](https://openweathermap.org/)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **API Keys** (https://home.openweathermap.org/api_keys)
4. Скопируйте ваш API ключ (или создайте новый)

### Шаг 2: Открытие проекта в Android Studio

1. Скачайте и установите [Android Studio](https://developer.android.com/studio) (если ещё не установлено)
2. Откройте Android Studio
3. Выберите **File → Open** (или **Open an Existing Project**)
4. Выберите папку `WeatherApp`
5. Дождитесь синхронизации Gradle (может занять несколько минут)

### Шаг 3: Настройка API ключа (опционально)

Вы можете добавить API ключ заранее в код:

1. Откройте файл `app/build.gradle.kts`
2. Найдите строку:
   ```kotlin
   buildConfigField("String", "OPENWEATHER_API_KEY", "\"YOUR_API_KEY_HERE\"")
   ```
3. Замените `YOUR_API_KEY_HERE` на ваш настоящий API ключ
4. Синхронизируйте проект (Sync Project with Gradle Files)

**Или** вы можете ввести API ключ при первом запуске приложения.

### Шаг 4: Запуск приложения

#### На эмуляторе:
1. Откройте **Device Manager** (Tools → Device Manager)
2. Создайте новый эмулятор (если нет):
   - Нажмите **Create Device**
   - Выберите устройство (например, Pixel 6)
   - Выберите образ системы (API 26 или выше)
   - Нажмите **Finish**
3. Запустите эмулятор
4. Нажмите **Run** (зелёная кнопка ▶) или `Shift + F10`

#### На реальном устройстве:
1. Включите **Режим разработчика** на телефоне:
   - Настройки → О телефоне → Нажмите 7 раз на "Номер сборки"
2. Включите **USB-отладку**:
   - Настройки → Для разработчиков → USB-отладка
3. Подключите телефон к компьютеру по USB
4. Выберите ваше устройство в Android Studio
5. Нажмите **Run** (зелёная кнопка ▶)

---

## 📂 Структура проекта

```
WeatherApp/
├── app/
│   ├── src/main/
│   │   ├── java/com/example/weatherapp/
│   │   │   ├── api/
│   │   │   │   ├── RetrofitClient.kt      # Настройка Retrofit
│   │   │   │   └── WeatherService.kt      # API интерфейс
│   │   │   ├── model/
│   │   │   │   ├── WeatherResponse.kt     # Модель текущей погоды
│   │   │   │   └── ForecastResponse.kt    # Модель прогноза
│   │   │   ├── repository/
│   │   │   │   └── WeatherRepository.kt   # Репозиторий данных
│   │   │   ├── ui/
│   │   │   │   ├── MainActivity.kt        # Главный экран
│   │   │   │   ├── SettingsActivity.kt    # Экран настроек
│   │   │   │   └── ForecastAdapter.kt     # Адаптер RecyclerView
│   │   │   └── utils/
│   │   │       ├── PreferencesManager.kt  # Менеджер настроек
│   │   │       └── NetworkUtils.kt        # Проверка сети
│   │   ├── res/
│   │   │   ├── layout/                    # XML разметки экранов
│   │   │   ├── values/                    # Ресурсы (цвета, строки, темы)
│   │   │   ├── values-night/              # Ресурсы для тёмной темы
│   │   │   ├── drawable/                  # Векторные иконки
│   │   │   └── menu/                      # Меню
│   │   └── AndroidManifest.xml
│   └── build.gradle.kts                   # Зависимости модуля
├── build.gradle.kts                       # Корневой build файл
├── settings.gradle.kts                    # Настройки Gradle
└── README.md                              # Этот файл
```

---

## 🔧 Используемые технологии

| Технология | Назначение |
|------------|------------|
| **Kotlin** | Язык программирования |
| **Retrofit** | HTTP-клиент для API запросов |
| **Gson** | Парсинг JSON |
| **Coroutines** | Асинхронные операции |
| **Glide** | Загрузка изображений |
| **Material Design 3** | UI компоненты |
| **SharedPreferences** | Хранение настроек |
| **ViewBinding** | Связывание views |

---

## 📖 Объяснение для защиты

### 1. Какой API используется?
Приложение использует **OpenWeather API** (https://openweathermap.org/api):
- Endpoint `/weather` - текущая погода
- Endpoint `/forecast` - 5-дневный прогноз (каждые 3 часа)

### 2. Какие данные получаются?
- Температура (текущая, мин, макс, ощущается как)
- Влажность (%)
- Скорость ветра (м/с или миль/ч)
- Атмосферное давление (гПа)
- Описание погоды и иконка

### 3. Где хранятся настройки?
В **SharedPreferences** хранятся:
- Выбранный город
- Единицы измерения (metric/imperial)
- Тема (светлая/тёмная/системная)
- Размер шрифта
- API ключ

### 4. Как реализованы темы?
- Используется **Material Design 3** с Day/Night темами
- Файл `values/themes.xml` - светлая тема
- Файл `values-night/themes.xml` - тёмная тема
- Переключение через `AppCompatDelegate.setDefaultNightMode()`

### 5. Как реализованы шрифты?
- Используется множитель размера (0.85 / 1.0 / 1.2)
- Применяется программно через `textSize = baseSize * multiplier`

---

## ⚠️ Возможные проблемы и решения

| Проблема | Решение |
|----------|---------|
| Gradle sync failed | Проверьте интернет-соединение, нажмите File → Invalidate Caches / Restart |
| API ключ не работает | Подождите 10-15 минут после регистрации, ключ активируется не сразу |
| Город не найден | Используйте английское название (Moscow, не Москва) |
| Нет иконок погоды | Проверьте подключение к интернету |

---

## 📝 Лицензия

Этот проект создан в учебных целях.

---

**Удачи на защите! 🎓**
